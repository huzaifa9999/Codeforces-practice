#include <bits/stdc++.h>
using namespace std;
int main()
{
    string s;
    string ans = "";
    cin >> s;

    int len = s.length();
    if(int(s[0])>=97)
    s[0]-=32;

cout<<s<<endl;

    return 0;
}